{
  "name": "dfl-one-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace=packages/product-registry",
    "build": "npm run build --workspace=packages/product-registry"
  }
}
